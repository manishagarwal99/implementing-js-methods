# implementing-js-methods

$ git init -b main

$ git add .

$ git commit -m "first commit"

$ git remote add origin https://github.com/manishagarwal99/implementing-js-methods.git

$ git push -u origin main
